export default class Form {
    constructor(word, meaning, sample, details) {
        this.word = word;
        this.meaning = meaning;
        this.sample = sample;
        this.details = details;
    }
}